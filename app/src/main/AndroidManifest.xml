<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.metalogy.fitapp">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitApp">
        <activity
            android:name=".activities.PullUpActivity"
            android:exported="false" />
        <activity
            android:name=".activities.VoiceStopwatchActivity"
            android:exported="false" />
        <activity
            android:name=".activities.SquatActivity"
            android:exported="false" />
        <activity
            android:name=".activities.PushUpActivity"
            android:exported="false" />
        <activity
            android:name=".activities.SelectionActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>